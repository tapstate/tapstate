# §14.8 — cross-database dual-source join -> report store (multi-source pipeline).
version: tapstate/v1
kind: source
id: src_crm
connector: mysql
config: { host: 10.10.0.4, username: cdc_user, password: Crm_2026 }
mode: cdc
tables: [ customers ]
