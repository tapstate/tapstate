# §14.5 — MySQL CDC -> Kafka (DB to event bus). Incremental only = pipeline read_mode (§1).
version: tapstate/v1
kind: source
id: src_my
connector: mysql
config: { host: 10.10.0.3, username: cdc_user, password: My_2026 }
mode: cdc
tables: [ orders ]
