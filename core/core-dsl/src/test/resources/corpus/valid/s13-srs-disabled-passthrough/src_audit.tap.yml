# read_mode amendment (§4): srs.enabled: false turns off the Shared Record Store; the
# cdc change stream goes straight to the single consumer with no shared buffering or replay.
version: tapstate/v1
kind: source
id: src_audit
connector: mysql
config: { host: 10.10.0.21, username: cdc_user, password: Adt_2026 }
mode: cdc
tables: [ audit_log ]
srs: { enabled: false }
