# §14.9 — conditional fan-out, v1 form: N pipelines x filter over one shared
# source (router left v1, X14). Single SRS mining chain, no duplicate source reads.
version: tapstate/v1
kind: source
id: src_orders
connector: mysql
config: { host: 10.10.0.5, username: cdc_user, password: Ord_2026 }
mode: cdc
tables: [ orders ]
