# A cdc-capable source. It can stream; whether this read has a tail is the pipeline's to say.
version: tapstate/v1
kind: source
id: src_ledger_sf
connector: mysql
config: { host: 10.10.0.21, username: cdc_user, password: Ldg_2026 }
mode: cdc
tables: [ ledger ]
