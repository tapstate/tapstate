# Invalid: references src_missing which is absent from this batch (closure = the batch
# in offline validate).
version: tapstate/v1
kind: pipeline
id: orphan_pipe
source: src_missing
serve:
  from: /.*/
  sync:
    - { id: out, source: tgt_pg, write_mode: upsert }
