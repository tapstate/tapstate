version: tapstate/v1
kind: pipeline
id: mysql2mongo
source: src_mysql
settings: { read_mode: snapshot_and_cdc }
transforms:
  - { id: rows_through, from: [orders], type: filter, expr: "op == 'r' || op == 'i'" }
serve:
  from: rows_through
  sync:
    - source: tgt_mongo
