version: tapstate/v1
kind: pipeline
id: e2e_pipeline
source: src_file
settings: { read_mode: snapshot_and_cdc }
transforms:
  - { id: all_orders, from: [orders], type: filter, expr: "true" }
serve:
  from: all_orders
  sync:
    - source: tgt_file
