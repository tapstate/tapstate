{
  "$id": "https://tapstate.io/schema/e2e/matchers-v1.json",
  "description": "Every word a specification may use. Generated from the executor; a word is admitted only once something real answers it.",
  "matchers": [
    {
      "word": "count",
      "description": "Rows present at an endpoint, read from the endpoint itself rather than from the product's record of what it wrote."
    },
    {
      "word": "dead_lettered",
      "description": "How many changes the pipeline's nests could never place in a document, added up over its namespaces and read from the metrics face. Nothing else says it: these rows were never going to appear in any document, so a pipeline discarding all of them and one discarding none have the same counts, state and code."
    },
    {
      "word": "doc",
      "description": "One document at an endpoint, located by equality settings and held to scalar values by path and list lengths by path - what makes 'the right rows crossed' assertable rather than only 'rows crossed'."
    },
    {
      "word": "error_count",
      "description": "The pipeline's published error count, read from the metrics face: one while it is FAILED, zero otherwise."
    },
    {
      "word": "failure_code",
      "description": "The canonical code of the failure the pipeline published, read from the status face: what killed the run, not just that it died."
    },
    {
      "word": "state",
      "description": "The pipeline's published lifecycle state, read from the observation face."
    }
  ],
  "steps": [
    {
      "word": "pause",
      "description": "A lifecycle verb. Written on its own it drives the pipeline; written with one source id it holds or releases that stream alone."
    },
    {
      "word": "resume",
      "description": "A lifecycle verb. Written on its own it drives the pipeline; written with one source id it holds or releases that stream alone."
    },
    {
      "word": "start",
      "description": "A lifecycle verb, driven on the pipeline. Written on its own."
    },
    {
      "word": "stop",
      "description": "A lifecycle verb, driven on the pipeline. Written on its own."
    },
    {
      "word": "restart",
      "description": "A word the terminal offers, driven on the pipeline. It expands into the product's own verbs; the vocabulary follows what a person types."
    },
    {
      "word": "restart --rerun",
      "description": "A word the terminal offers, driven on the pipeline. It expands into the product's own verbs; the vocabulary follows what a person types."
    },
    {
      "word": "assert",
      "description": "Checks a matcher once, now."
    },
    {
      "word": "await",
      "description": "Polls a matcher until it holds or the bound expires."
    },
    {
      "word": "cdc",
      "description": "Produces changes against a seeded table while the pipeline runs."
    }
  ],
  "cdcOperations": [
    {
      "word": "delete",
      "description": "Produces delete changes."
    },
    {
      "word": "insert",
      "description": "Produces insert changes."
    },
    {
      "word": "update",
      "description": "Produces update changes."
    }
  ],
  "pipelineStates": [
    "COMPLETED",
    "FAILED",
    "NEW",
    "PAUSED",
    "RUNNING",
    "STOPPED"
  ],
  "topLevelKeys": [
    "name",
    "setup",
    "pipeline",
    "seed",
    "steps"
  ],
  "setupKeys": [
    "databases",
    "connectors",
    "apply",
    "discover"
  ],
  "databaseKinds": [
    {
      "word": "mongo",
      "description": "Provides a mongo store."
    },
    {
      "word": "mysql",
      "description": "Provides a mysql store."
    },
    {
      "word": "postgres",
      "description": "Provides a postgres store."
    }
  ]
}
