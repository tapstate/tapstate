{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tapstate.io/schema/e2e/spec-v1.json",
  "title": "Tapstate e2e test specification",
  "description": "A declarative end-to-end specification. The same document runs on every tier; generated from the executor, so it says what the parser accepts.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "pipeline",
    "steps"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "What this specification is called."
    },
    "setup": {
      "$ref": "#/$defs/setup",
      "description": "The bootstrap a real endpoint needs before a pipeline can name it."
    },
    "pipeline": {
      "type": "string",
      "description": "Path to the product pipeline document, read with the product's own parser."
    },
    "seed": {
      "$ref": "#/$defs/seed",
      "description": "Initial data laid down before the first step runs."
    },
    "steps": {
      "type": "array",
      "description": "The scenario, in order. A specification with no steps checks nothing.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/step"
      }
    }
  },
  "$defs": {
    "setup": {
      "type": "object",
      "description": "What the harness brings up, then the three product verbs run against it, in dependency order.",
      "additionalProperties": false,
      "properties": {
        "databases": {
          "type": "object",
          "description": "Stores the harness provisions first, keyed by the name whose address the resources interpolate.",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "description": "What sort of store, which settles provisioning, address shape and driver.",
                "enum": [
                  "mongo",
                  "mysql",
                  "postgres"
                ]
              }
            }
          }
        },
        "connectors": {
          "type": "array",
          "description": "Connector ids whose runtime jars are registered; idempotent by content hash.",
          "items": {
            "type": "string"
          }
        },
        "apply": {
          "type": "array",
          "description": "Product resource files, applied as one batch: the product resolves references within the submitted set. The pipeline is applied in the same batch whether or not it is listed here.",
          "items": {
            "type": "string"
          }
        },
        "discover": {
          "type": "array",
          "description": "Resource ids whose source model is discovered, feeding target-table creation.",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "seed": {
      "type": "object",
      "description": "Rows per table, addressed as <resourceId>.<table>.",
      "propertyNames": {
        "pattern": "^[^.]+\\..+$"
      },
      "additionalProperties": {
        "description": "A generated count, or the rows themselves - one of the two.",
        "oneOf": [
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "rows"
            ],
            "properties": {
              "rows": {
                "type": "integer",
                "description": "How many generated rows to lay down: ids 1..N, each with seq equal to its id.",
                "minimum": 0
              }
            }
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "values"
            ],
            "properties": {
              "values": {
                "type": "array",
                "description": "The rows themselves, when what they hold is the point.",
                "minItems": 1,
                "items": {
                  "type": "object",
                  "description": "One row: columns and values. Every row of a table carries the same columns.",
                  "required": [
                    "id"
                  ],
                  "properties": {
                    "id": {
                      "type": "integer",
                      "description": "The key rows are seeded and upserted by."
                    }
                  },
                  "additionalProperties": {
                    "type": [
                      "integer",
                      "string"
                    ],
                    "description": "An integer or a string; wider value types are a widening of the vocabulary."
                  }
                }
              }
            }
          }
        ]
      }
    },
    "step": {
      "description": "One stage. Steps run in declaration order; the order is the scenario.",
      "oneOf": [
        {
          "type": "string",
          "description": "A step written on its own: a lifecycle verb spelled as the product spells it, or a word the terminal offers that expands into several of them. There is no rewind: re-snapshotting is stop then start, which is what restart --rerun does.",
          "enum": [
            "pause",
            "restart",
            "restart --rerun",
            "resume",
            "start",
            "stop"
          ]
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "cdc"
          ],
          "properties": {
            "cdc": {
              "type": "object",
              "description": "Exactly one table, addressed as <resourceId>.<table>.",
              "minProperties": 1,
              "maxProperties": 1,
              "propertyNames": {
                "pattern": "^[^.]+\\..+$"
              },
              "additionalProperties": {
                "description": "A change. Written '<operation> <rows>' the driver chooses the rows; written as an operation with where (and set, for an update) the specification chooses them.",
                "oneOf": [
                  {
                    "type": "string",
                    "description": "A change, written '<operation> <rows>'.",
                    "pattern": "^(delete|insert|update)\\s+\\d+$"
                  },
                  {
                    "description": "A change against rows the specification names.",
                    "oneOf": [
                      {
                        "type": "object",
                        "additionalProperties": false,
                        "required": [
                          "delete"
                        ],
                        "properties": {
                          "delete": {
                            "type": "object",
                            "additionalProperties": false,
                            "required": [
                              "where"
                            ],
                            "properties": {
                              "where": {
                                "type": "object",
                                "description": "Equality settings locating exactly one row. Identity is spelled id whatever the store calls it.",
                                "minProperties": 1,
                                "additionalProperties": {
                                  "type": [
                                    "integer",
                                    "string"
                                  ],
                                  "description": "An integer or a string; wider value types are a widening of the vocabulary."
                                }
                              }
                            }
                          }
                        }
                      },
                      {
                        "type": "object",
                        "additionalProperties": false,
                        "required": [
                          "insert"
                        ],
                        "properties": {
                          "insert": {
                            "type": "object",
                            "additionalProperties": false,
                            "required": [
                              "values"
                            ],
                            "properties": {
                              "values": {
                                "type": "array",
                                "description": "The rows to add, spelled the way a seed spells them.",
                                "minItems": 1,
                                "items": {
                                  "type": "object",
                                  "description": "One row: its columns and their values, carrying id.",
                                  "minProperties": 1,
                                  "required": [
                                    "id"
                                  ],
                                  "additionalProperties": {
                                    "type": [
                                      "integer",
                                      "string"
                                    ],
                                    "description": "An integer or a string; wider value types are a widening of the vocabulary."
                                  }
                                }
                              }
                            }
                          }
                        }
                      },
                      {
                        "type": "object",
                        "additionalProperties": false,
                        "required": [
                          "update"
                        ],
                        "properties": {
                          "update": {
                            "type": "object",
                            "additionalProperties": false,
                            "required": [
                              "where",
                              "set"
                            ],
                            "properties": {
                              "where": {
                                "type": "object",
                                "description": "Equality settings locating exactly one row. Identity is spelled id whatever the store calls it.",
                                "minProperties": 1,
                                "additionalProperties": {
                                  "type": [
                                    "integer",
                                    "string"
                                  ],
                                  "description": "An integer or a string; wider value types are a widening of the vocabulary."
                                }
                              },
                              "set": {
                                "type": "object",
                                "description": "Columns to write on the located row, by name.",
                                "minProperties": 1,
                                "additionalProperties": {
                                  "type": [
                                    "integer",
                                    "string"
                                  ],
                                  "description": "An integer or a string; wider value types are a widening of the vocabulary."
                                }
                              }
                            }
                          }
                        }
                      }
                    ]
                  }
                ]
              }
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "await"
          ],
          "properties": {
            "await": {
              "$ref": "#/$defs/matcher"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "assert"
          ],
          "properties": {
            "assert": {
              "$ref": "#/$defs/matcher"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "pause"
          ],
          "properties": {
            "pause": {
              "type": "string",
              "description": "The id of the source whose stream this holds or releases, leaving every other stream of the same pipeline running.",
              "minLength": 1
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "resume"
          ],
          "properties": {
            "resume": {
              "type": "string",
              "description": "The id of the source whose stream this holds or releases, leaving every other stream of the same pipeline running.",
              "minLength": 1
            }
          }
        }
      ]
    },
    "matcher": {
      "description": "A condition over observable state. One vocabulary serves both timings: assert reads once, await polls the same matcher until it holds or the bound expires.",
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "count"
          ],
          "properties": {
            "count": {
              "type": "object",
              "description": "Expected rows per table, read from the endpoint itself.",
              "minProperties": 1,
              "propertyNames": {
                "pattern": "^[^.]+\\..+$"
              },
              "additionalProperties": {
                "type": "integer",
                "description": "Rows the table is expected to hold.",
                "minimum": 0
              }
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "doc"
          ],
          "properties": {
            "doc": {
              "type": "object",
              "description": "Exactly one table, addressed as <resourceId>.<table>.",
              "minProperties": 1,
              "maxProperties": 1,
              "propertyNames": {
                "pattern": "^[^.]+\\..+$"
              },
              "additionalProperties": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "where"
                ],
                "description": "One document, located and read at the endpoint itself. Carry expect or size - a doc that expects nothing checks nothing.",
                "properties": {
                  "where": {
                    "type": "object",
                    "description": "Equality settings locating exactly one document. Identity is spelled id whatever the store calls it.",
                    "minProperties": 1,
                    "additionalProperties": {
                      "type": [
                        "integer",
                        "string"
                      ],
                      "description": "An integer or a string; wider value types are a widening of the vocabulary."
                    }
                  },
                  "expect": {
                    "type": "object",
                    "description": "Scalar values by path: a.b for a field of a field, items[0].sku for a field of a list element.",
                    "additionalProperties": {
                      "type": [
                        "integer",
                        "string"
                      ],
                      "description": "An integer or a string; wider value types are a widening of the vocabulary."
                    }
                  },
                  "size": {
                    "type": "object",
                    "description": "List lengths by path.",
                    "additionalProperties": {
                      "type": "integer",
                      "description": "How many elements the list at this path holds.",
                      "minimum": 0
                    }
                  }
                },
                "anyOf": [
                  {
                    "required": [
                      "expect"
                    ]
                  },
                  {
                    "required": [
                      "size"
                    ]
                  }
                ]
              }
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "error_count"
          ],
          "properties": {
            "error_count": {
              "type": "integer",
              "description": "The published error count the pipeline is expected to show.",
              "minimum": 0
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "dead_lettered"
          ],
          "properties": {
            "dead_lettered": {
              "type": "integer",
              "description": "How many changes this pipeline's nests are expected to have been unable to place in a document, added up over its namespaces. Zero is a real assertion here rather than a default: a pipeline that discarded rows publishes a number instead.",
              "minimum": 0
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "failure_code"
          ],
          "properties": {
            "failure_code": {
              "type": "string",
              "description": "The canonical code of the failure the pipeline this specification names is expected to publish, written '<domain>.<symbol>'.",
              "pattern": "^[a-z0-9]+\\.[a-z0-9-]+$"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "state"
          ],
          "properties": {
            "state": {
              "type": "string",
              "description": "The lifecycle state of the pipeline this specification names. A specification names exactly one, so the state is written on its own.",
              "enum": [
                "COMPLETED",
                "FAILED",
                "NEW",
                "PAUSED",
                "RUNNING",
                "STOPPED"
              ]
            }
          }
        }
      ]
    }
  }
}
