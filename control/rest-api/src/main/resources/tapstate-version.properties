# Filtered from the reactor at build time. The one place this face learns the product version, so it
# tracks the single source of truth instead of being a second copy someone has to remember to bump.
version=${project.version}
